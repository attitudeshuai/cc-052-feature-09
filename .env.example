# Database
DB_HOST=db
DB_PORT=5432
DB_USER=farm
DB_PASSWORD=farm_secret
DB_NAME=farm_trace

# Redis
REDIS_HOST=cache
REDIS_PORT=6379
REDIS_PASSWORD=

# MinIO
MINIO_ENDPOINT=minio:9000
MINIO_PUBLIC_ENDPOINT=localhost:19080
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=farm-trace
MINIO_USE_SSL=false

# Server
SERVER_PORT=8160

# 扫码地区服务端解析：网段→行政区划代码映射（分号分隔）。
# 只按真实对端 IP 查此映射，匹配不到记为未知；绝不采信客户端自报的来源。
# GEO_REGION_MAP=203.0.113.0/24=110105;198.51.100.0/24=310104
GEO_REGION_MAP=
